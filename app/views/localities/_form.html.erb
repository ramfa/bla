<%= form_for(@locality) do |f| %>
  <% if @locality.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@locality.errors.count, "error") %> prohibited this locality from being saved:</h2>

      <ul>
      <% @locality.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :provincia %><br>
    <%= f.select :province_id, Province.all.map { |p|[p.name,p.id]  },options ={:prompt => "Seleccione una localidad"} %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
