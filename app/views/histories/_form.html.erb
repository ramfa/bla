<%= form_for(@history) do |f| %>
  <% if @history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@history.errors.count, "error") %> prohibited this history from being saved:</h2>

      <ul>
      <% @history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :animal_id %><br>
    <%= f.grouped_collection_select(:animal_id, @commonnames, :animals, :name, :id, :name , options ={:prompt => "Seleccione un Animal"} ) %>
  </div>
  <div class="field">
    <%= f.label :tratamiento_id %><br>
    <%= f.select :tratamiento_id, Tratamiento.all.map { |t|[t.signos_clinicos,t.id]  },options ={:prompt => "Seleccione un Tratamiento"} %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
